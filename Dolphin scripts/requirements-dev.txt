add-trailing-comma>=2.4.0  # Added support for with statement
autopep8>=2.0.2 # New checks # Must match .pre-commit-config.yaml
pre-commit
ruff>=0.0.277 # New Bandit checks and fixed issue # Must match .pre-commit-config.yaml
